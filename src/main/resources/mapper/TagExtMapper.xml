<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.memoforward.blog.mapper.TagExtMapper">
    <resultMap id="BaseResultMap" type="com.memoforward.blog.model.Tag">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Mon Apr 13 10:36:09 CST 2020.
        -->
        <id column="id" jdbcType="BIGINT" property="id"/>
        <result column="name" jdbcType="VARCHAR" property="name"/>
    </resultMap>

    <select id="listBlogTags" parameterType="java.lang.Long" resultMap="BaseResultMap">
        SELECT t.id, t.name
        from t_tag t
                 inner join t_blog_tags s
                            on t.id = s.tag_id and s.blog_id = #{blogId};
    </select>

    <select id="listTopTags" parameterType="java.lang.Integer" resultType="com.memoforward.blog.dto.TagDto">
        SELECT count(*) as count, t.name as name
        from t_tag t
                 INNER JOIN t_blog_tags s on t.id = s.tag_id
        GROUP BY s.tag_id
        ORDER BY count(*) desc
        limit #{limit} offset 0;
    </select>
</mapper>